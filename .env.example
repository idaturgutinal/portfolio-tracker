# ─── Database ────────────────────────────────────────────────────────────────
# For local development: SQLite file
DATABASE_URL="file:./dev.db"

# For Vercel / production: use a hosted database (e.g. Neon PostgreSQL)
# DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# ─── Auth ─────────────────────────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-here"

# Set to your production URL in deployment (Vercel sets this automatically)
# NEXTAUTH_URL="https://your-app.vercel.app"

# ─── Market Data (optional) ───────────────────────────────────────────────────
# Yahoo Finance is used by default — no key required.
# Provide an Alpha Vantage key as a fallback data source (free tier: 25 req/day).
# Sign up at: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=""
